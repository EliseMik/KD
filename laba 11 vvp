#include<iostream>
#include<cmath>
using namespace std;


//laba 11.1

/*
int main()
{
	int a, b;
	cout<< "Enter two integer type variables: A and B." <<endl;
	cin >> a >> b;
	if (a == b)
	{
		a = 0;
		b = 0;
	}
	else
	{
		a = max(a, b);
		b = max(a, b);
	}
	cout<< "new values"<<endl;
	cout << "a =" << a << ", b = " << b;
	return 0;
}
*/

//laba 11.2

/*
int main()
{
	int a, b, c;
	cout<< "Enter 3 integer type variables." <<endl;
	cin >> a >> b >> c;
	cout << "the sum of two large numbers is " << a + b + c - min(min(a, b), min(b, c));
}
*/

//laba 11.3

/*
int main()
{
	float a1, a2, b1, b2, c1, c2, ac, ab;
	
	cout<< "enter three coordinates of type (x, y): A, B, C." <<endl;
	cin >> a1 >> a2 >> b1 >> b2 >> c1 >> c2;
	ab = sqrt((a1 - b1) * (a1 - b1) + (a2 - b2) * (a2 - b2));
	ac = sqrt((a1 - c1) * (a1 - c1) + (a2 - c2) * (a2 - c2));
	if (ab < ac)
	{
		cout <<" the point "B" is closer to the point A :" << "b" << " " << ab;
	}
	else
	{
		cout <<" the point "C" is closer to the point A :" << "c" << " " << ac;
	}
}
*/

//laba 11.4

/*
int main()
{
	int x, y;
	cout<< "enter the point coordinate" <<endl;
	cin >> x >> y;
	cout<< "the point is in the quarter: ";
	if (x > 0)
	{
		if (y > 0)
		{
			cout << "1" << endl;
		}
		else
		{
			cout << "4" << endl;
		}
	}
	else
	{
		if (y > 0)
		{
			cout << "2" << endl;
		}
		else
		{
			cout << "3" << endl;
		}
	}
}
*/


//laba 11.5

/*
int main()
{
	int x;
	cout<< "enter integer number"<<endl;
	cin >> x;
	if (x == 0)
	{
		cout << "zero ";
	}
	else
	{
		if (x > 0)                           //если число больше 0
		{
			cout << "positive ";
		}
		else
		{
			cout << "negative ";
		}
		if (x % 2 == 0)                      //если число четное
		{
			cout << "even ";
		}
		else
		{
			cout << "odd ";
		}
	}
	cout << "number" << endl;
	return 0;

}
*/

//laba 11.6

/*
int main()
{
	int x;
	cout<< "enter the number 1-999"<<endl;
	cin >> x;
	if (x % 2 == 0)                           //если число четное
	{
		cout << "even ";
	}
	else
	{
		cout << "odd ";
	}
	if (x / 100 != 0)                          //если число при делении на сто больше нуля
	{
		cout << "three-digit ";
	}
	else
	{
		if (x / 10 != 0)                 //если число при делении на десять больше нуля
		{
			cout << "two-digit ";
		}
		else
		{
			cout << "one-figure ";
		}
	}
	cout << "number";
}
*/
